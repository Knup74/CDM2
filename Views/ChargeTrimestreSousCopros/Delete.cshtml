@model CDM.Database.Models.ChargeTrimestreSousCopro

@{
    ViewData["Title"] = "Supprimer une association";
}

<div class="alert alert-danger shadow-sm">
    <h4 class="alert-heading">Supprimer cette association ?</h4>

    <ul>
        <li><strong>Charge :</strong> @Model.ChargeTrimestre.Libelle</li>
        <li><strong>Trimestre :</strong> @Model.ChargeTrimestre.Trimestre.Annee - T@Model.ChargeTrimestre.Trimestre.Numero</li>
        <li><strong>Sous-copropriété :</strong> @Model.SousCopropriete.Nom</li>
    </ul>
</div>

<form asp-action="DeleteConfirmed">
    <input type="hidden" name="chargeTrimestreId" value="@Model.ChargeTrimestreId" />
    <input type="hidden" name="sousCoproprieteId" value="@Model.SousCoproprieteId" />

    <button type="submit" class="btn btn-danger">Supprimer</button>
    <a asp-action="Index" class="btn btn-secondary">Annuler</a>
</form>
